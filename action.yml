name: .NET publish GitPackage

on:
  push:
    branches: [ main ]
env:
  DIRECTORY_SRC: './src'
  BUILD_CONFIGURATION: 'Release'
  GITHUB_REPOSITORY_OWNER: architecture-it 
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Add NuGet sources
      run: |
        dotnet nuget locals -c all       
        dotnet nuget add source https://nuget.pkg.github.com/${{ env.GITHUB_REPOSITORY_OWNER }}/index.json --name github -u ${{ secrets.ARQUITECTURA_USER }} -p ${{ secrets.ARQUITECTURA_DEPLOY  }} --store-password-in-clear-text
    - name: dotnet build and publish
      run: |
        dotnet restore
        dotnet build --configuration '${{ env.BUILD_CONFIGURATION }}'
        dotnet pack -c '${{ env.BUILD_CONFIGURATION }}' --version-suffix $GITHUB_RUN_ID
      working-directory: ${{ env.DIRECTORY_SRC }}
    - name: Nuget Push on Github
      run: dotnet nuget push --source github --api-key ${{ secrets.ARQUITECTURA_DEPLOY }} --skip-duplicate bin/Release/*.nupkg
      working-directory: ${{ env.DIRECTORY_SRC }}
