name: "Sonar Analizer Javascript/Typescript for NextJs Apps"
description: "Realiza el análisis de sonarqube para aplicaciones en NextJs"
inputs:
  sonar_url: 
    description: "url sonarqube"
    required: true
  sonar_token: 
    description: "token sonarqube"
    required: true
  sonar_key:
    description: "Key del repositorio"
    required: true
  skip_test:
    description: "permite skipear los procesos de testing"
    required: false
    default: 'false' 
runs:
  using: composite
  steps:
    - shell: bash
      run: |
        echo "ACTION SONAR JS-nextjs LOADED"
        exit 0
